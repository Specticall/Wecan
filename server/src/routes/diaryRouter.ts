import { Router } from "express";

const router = Router();

// Retrieve user's diaries
router.get("/");

// Create a new diary
router.post("/");

// Delete user's diary
router.delete("/");

export default router;
